# Assumptions

- Ambiguous timestamps assumed in IST (UTC+05:30).
- `facility_crs_raipur.csv` is treated as an input feature source and not recomputed in this phase.
- `weather_snapshot_*.json` are deterministic replay inputs for scenario simulation/CI.
